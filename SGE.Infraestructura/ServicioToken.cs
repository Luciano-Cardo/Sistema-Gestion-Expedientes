namespace SGE.Infraestructura;

using SGE.Aplicacion.Servicios;

public record class ServicioToken : ITokenService
{
    public String GenerarToken(Guid Id)
    {
        //mas adelante
        return 
    }
}